<%= render 'form', reservation: @reservation %>

<% if @reservation.errors.any? == false %>

<%= link_to("Delete", edit_reservation_path(@reservation), method: :delete, data: {confirm: "Are you sure you want to delete this restaurant?"})%>
<%end %>
